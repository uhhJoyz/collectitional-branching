This section of the codebase is for the map part of the algorithm.

# Building

If you are using MacOS, be sure to set the DARWIN environment variable with `export DARWIN=1`.

```bash
# starting in ./mapping-algorithm
mkdir build
cmake ..
make -j4
```
